/*-----------------------------------------------------------------------------
-------------------------------------------------------------------------------
                            |
         FM-Pool            |  Optaphy: Thermoelectric Logistics Management
          9173              |  www.optaphy.com
                            |
-------------------------------------------------------------------------------
   Copyright (C) 2024 Optaphy SE
-------------------------------------------------------------------------------
-----------------------------------------------------------------------------*/

// Header file for input output functions
#include <stdio.h>

int main() {
    // Print a character
    printf("~\n");

    // Logistics and ERP-related constants (initialized for now)
    const float abd = 0.0f;   // Agency Business Document
    const float cass = 0.0f;  // Cargo Account Settlement System
    const float crm = 0.0f;   // Customer Relationship Management
    const float dtr = 0.0f;   // Delivery-based Transportation Requirement
    const float ecc = 0.0f;   // ERP Core Component
    const float fa = 0.0f;    // Freight Agreement
    const float fb = 0.0f;    // Freight Booking
    const float fo = 0.0f;    // Freight Order
    const float fsd = 0.0f;   // Freight Settlement Document
    const float fu = 0.0f;    // Freight Unit
    const float fubr = 0.0f;  // Freight Unit Building Rule
    const float fwo = 0.0f;   // Forwarding Order
    const float fwsd = 0.0f;  // Forwarding Settlement Document
    const float lsp = 0.0f;   // Logistics Service Provider
    const float otr = 0.0f;   // Order-based Transportation Requirement
    const float pi = 0.0f;    // Process Integration
    const float ses = 0.0f;   // Service Entry Sheet
    const float trq = 0.0f;   // Transportation Requirement
    const float vsr = 0.0f;   // Vehicle Scheduling and Routing

    return 0;
}


